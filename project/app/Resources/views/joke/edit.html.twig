{% extends 'base.html.twig' %}

{% block body %}
    <div class="content">
        <div class="pricing-header px-3 py-3 pt-md-5 pb-md-4 mx-auto text-center">
            <h1 class="display-4">Изменение анекдота</h1>
        </div>
        <div class="container">
            {{ form_start(edit_form) }}
            {{ form_widget(edit_form) }}
            <input type="submit" class="btn btn-success btn-lg" value="Изменить"/>
            <a class="btn btn-danger btn-lg" href="{{ path('joke_delete', {'id':joke.id}) }}">Удалить</a>
            <a class="btn btn-secondary btn-lg" href="{{ path('joke_index') }}">Вернуться к списку</a>

        </div>
    </div>

{% endblock %}
